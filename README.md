# Distillation-Column-Optimization
This is an artificial neural network trained to predict ethanol concentration values using a distillation column dataset. The ANN is optimized to find the highest ethanol concentration value and the parameters that produce this value.
